# empty to disable codeql until it's introduced again

name: CodeQL
on:
  schedule:
    - cron: "0 0 * * 0"  # Run every Sunday at midnight UTC
# runs:
#     - name
jobs:
  build-java:
    name: Build Java
    runs-on: ubuntu-latest
    steps: 
      - name: Build with Gradle
        run: ./gradlew build
